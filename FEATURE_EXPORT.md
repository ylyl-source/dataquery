数据导出功能模块 - 新增功能设计文档
一、功能概述
在现有智能数据库查询工具基础上，新增查询结果数据导出模块。用户执行数据库查询得到结果集之后，可一键将查询结果导出为本地文件；同时接入 AI‑Agent 自动化能力，支持自然语言触发导出、查询‑导出一键完成的自动化流程。
支持导出格式：CSV、JSON。
二、设计思路
2.1 整体架构思路
功能采用解耦设计，新增独立导出模块，不侵入原有查询核心逻辑。
完整链路：
用户发起自然语言查询 → AI 生成 SQL → 数据库执行查询 → 获取结果数据集 → 导出模块接收数据集 → 文件生成下载 → 交互反馈
导出模块作为独立子服务，接收上游查询返回的结构化结果数据，完成格式转换、文件生成、下载返回；查询模块与导出模块之间通过数据集对象解耦，后续可轻松扩展 Excel 等更多导出格式。
2.2 导出格式方案设计（CSV + JSON）
JSON 格式：直接将查询返回的列表‑字典原始结果序列化输出，完整保留字段类型、嵌套结构，适合程序二次处理。
CSV 格式：将查询结果二维表格化处理，第一行写入表头（数据库字段名），后续逐行写入数据；对包含逗号、换行符的字段做转义处理，避免 CSV 文件错乱。
文件命名规则：自动生成带时间戳的文件名，示例：query_result_20260818_153022.csv
2.3 自动化一键查询‑导出流程（Agent / Command）
利用 Cursor / Claude Code Agent 自定义命令实现一键工作流，提供两种触发模式：
模式 A：单条自然语言命令一键完成
用户直接输入一句话即可同时执行查询 + 导出，示例：
查询 2026 年订单数据并导出为 CSV 文件
Agent 自动拆解任务步骤：
解析用户自然语言，提取查询条件 + 目标导出格式
生成并执行对应 SQL 语句
获取查询结果集
自动调用导出模块生成对应文件
返回文件下载链接，完成全流程
模式 B：分步触发（先查询，后导出）
用户先执行数据库查询，得到结果
AI 助手主动交互询问导出意向：需要将这次查询结果导出为 CSV 或 JSON 文件吗？
用户回复确认格式后，Agent 触发导出动作，生成并返回文件
2.4 用户交互方案
1）自然语言交互触发（对话式）
支持口语化指令唤起导出功能，示例指令：
“把刚刚查询的数据导出 csv”
“将上一次结果保存成 json 文件”
Agent 识别上下文，读取最近一次查询结果集，执行导出。
2）界面按钮操作触发（可视化）
在查询结果展示面板，新增导出按钮组：
【导出 CSV】｜【导出 JSON】
用户点击按钮即可立即下载当前表格结果。
3）AI 主动弹窗询问（自动化交互）
每次成功执行查询并且返回非空结果集后，AI 助手主动发起导出询问；
当查询结果为空时，跳过导出询问，提示 “无查询数据，不可导出”。
三、边界与异常处理设计
空结果校验：查询无数据，禁止导出文件，返回提示信息
大数据量防护：增加导出行数上限提示，超大量结果给出风险提醒
上下文校验：自然语言导出指令时，校验是否存在最近一次查询数据集，无结果时提示用户先执行查询
文件转义容错：CSV 导出自动转义特殊字符，防止文件损坏
四、后续可扩展方向
新增导出格式：Excel（.xlsx）
导出任务后台异步处理，支持超大结果集导出
导出文件保存路径自定义配置
